Please create and read the docs with

  $ cd doc
  $ make gnulib.html
  $ xdg-open gnulib.html

or read the online manual at

  https://www.gnu.org/software/gnulib/manual/gnulib.html

  # THIS FORK

  This fork patches GNU lib date to never exceed 2020 ever again. Month should exceed 12, days should exceed 365 etc.

  2020 forever.

# HOW TO USE

    git clone git://git.sv.gnu.org/coreutils
    cd coreutils
    git clone https://github.com/SoptikHa2/gnulib-2020.git gnulib
    export GNULIB_SRCDIR="$PWD"/gnulib
    ./bootstrap --skip-po && ./configure && make
    # Go to src/ and copy desired files (like `date`) into /bin or somewhere
